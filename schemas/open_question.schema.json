{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://thecruelsong.com/schemas/open_question.schema.json",
  "title": "Karmein Chan Knowledge File — open questions module",
  "description": "Validates the complete data/open_questions.json wrapper, embedded source descriptors and open-question records.",
  "type": "object",
  "required": [
    "file",
    "schema_version",
    "updated",
    "claim_model",
    "sources",
    "open_questions"
  ],
  "properties": {
    "file": {
      "const": "open_questions.json"
    },
    "schema_version": {
      "$ref": "#/$defs/semver"
    },
    "updated": {
      "$ref": "#/$defs/isoDate"
    },
    "claim_model": {
      "$ref": "#/$defs/claim"
    },
    "sources": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/embeddedSource"
      }
    },
    "open_questions": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/openQuestionRecord"
      }
    }
  },
  "additionalProperties": false,
  "$defs": {
    "nonEmptyString": {
      "type": "string",
      "minLength": 1
    },
    "semver": {
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d+$"
    },
    "isoDate": {
      "type": "string",
      "pattern": "^\\d{4}-\\d{2}-\\d{2}$"
    },
    "sourceRef": {
      "type": "string",
      "pattern": "^(?:KCSRC\\d{5}|TCS-WWKC-WEB-\\d{4}|\\.\\.\\.)$"
    },
    "personId": {
      "type": "string",
      "pattern": "^TCS-WWKC-PPL-\\d{4}$"
    },
    "placeId": {
      "type": "string",
      "pattern": "^KCPLACE\\d{4}$"
    },
    "eventId": {
      "type": "string",
      "pattern": "^KCEVT\\d{4}$"
    },
    "questionId": {
      "type": "string",
      "pattern": "^KCQST\\d{4}$"
    },
    "reviewFlags": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/nonEmptyString"
      },
      "uniqueItems": true
    },
    "claim": {
      "type": "object",
      "description": "Atomic value with semantic type, precision, inference score and supporting source references.",
      "required": [
        "value",
        "units",
        "precision",
        "inference_score",
        "source_ids"
      ],
      "properties": {
        "value": {},
        "units": {
          "type": [
            "string",
            "null"
          ]
        },
        "precision": {
          "$ref": "#/$defs/nonEmptyString"
        },
        "inference_score": {
          "type": "integer",
          "minimum": 0,
          "maximum": 3
        },
        "source_ids": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/sourceRef"
          },
          "uniqueItems": true
        }
      },
      "additionalProperties": false
    },
    "embeddedSource": {
      "type": "object",
      "required": [
        "source_id",
        "title",
        "url",
        "locators"
      ],
      "properties": {
        "source_id": {
          "$ref": "#/$defs/sourceRef"
        },
        "title": {
          "$ref": "#/$defs/nonEmptyString"
        },
        "url": {
          "type": "string",
          "format": "uri"
        },
        "locators": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/nonEmptyString"
          },
          "uniqueItems": true
        }
      },
      "additionalProperties": false
    },
    "openQuestionRecord": {
      "type": "object",
      "required": [
        "id",
        "record_type",
        "category",
        "question",
        "status",
        "source_locator",
        "related_person_ids",
        "related_place_ids",
        "related_event_ids",
        "review_flags"
      ],
      "properties": {
        "id": {
          "$ref": "#/$defs/questionId"
        },
        "record_type": {
          "const": "open_question"
        },
        "category": {
          "$ref": "#/$defs/claim"
        },
        "question": {
          "$ref": "#/$defs/claim"
        },
        "status": {
          "$ref": "#/$defs/claim"
        },
        "source_locator": {
          "$ref": "#/$defs/claim"
        },
        "related_person_ids": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/personId"
          },
          "uniqueItems": true
        },
        "related_place_ids": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/placeId"
          },
          "uniqueItems": true
        },
        "related_event_ids": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/eventId"
          },
          "uniqueItems": true
        },
        "review_flags": {
          "$ref": "#/$defs/reviewFlags"
        }
      },
      "additionalProperties": false
    }
  }
}
